{
  "command-runner.commands": {
    "create env": "conda create -n parse python=3.9 -y",
    "install requirements": "conda activate parse && pip install -r requirements.txt",
    "run": "conda activate parse && python main.py"
  }
}
